# 3S03-a1

All source files for quesitons 3, 4, and 5, can be found in the maven project found under the "assignment1" repository.

Ensure you have **Maven** and **JDK 21** installed on your system.

### 1. Compile the Project
From the root directory of the `assignment1` project, run:
```bash
mvn clean compile